# Each app manages its own node_modules/.next/etc via its own .gitignore
# (ai-ma-radar/.gitignore, world-history-map/.gitignore). This root-level
# file only catches stray build artifacts if a tool is ever run from the
# repo root by mistake.
node_modules/
.next/
*.tsbuildinfo
.DS_Store
